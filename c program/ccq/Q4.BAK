#include <stdio.h>
#include <conio.h>
#include <string.h>

void words(char *word1, char *word2)
{
int len1=strlen(word1);
int len2=strlen(word2);

if (len1!=len2)
{
getch();
}
for (int i=0; i < len1; i++)
{
if (word1[i] != word2[i])
{
getch();
}
}
getch();
}
void main() {
char word1[100];
char word2[100];
printf("Enter the first word: ");
scanf("%s", word1);
printf("Enter the second word: ");
scanf("%s", word2);
if (words(char word1 , char word2))
{
printf("The words are the same.\n");
}
else
{
printf("The words are not the same.\n");
}
getch();
}
